from sqlalchemy.ext.asyncio import AsyncSession
from app.models.conversation import Conversation


# Placeholder — add query methods here as needed
# Example:
# async def create_conversation(db: AsyncSession, instagram_user_id: str, message: str) -> Conversation:
#     obj = Conversation(instagram_user_id=instagram_user_id, message=message)
#     db.add(obj)
#     await db.commit()
#     await db.refresh(obj)
#     return obj
